最初，ECS告诉我slice不能具有动态大小的对象。
但是，JS本身都在堆上，内存连续性是不存在的。
后来，副slice的目的是将这些“变动的关系”提取出来
再后来，发现这玩意有索引的作用。

数据库里，索引是为了加速where查询。
作为索引，副slice很合适。

副slice现在被认为还能具有这些作用：
反查表。
轻量加速缓存：比如聚合统计。除非性能瓶颈，否则F自己现场统计，还能避免过期
重量级的聚合视图要靠F/后端

副slice的数据能从主slice完全推导
尽可能只存id或id的列表